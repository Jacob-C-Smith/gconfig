#version 450 

layout (local_size_x = 10, local_size_y = 10, local_size_z = 1) in;

layout(rgba32f, binding = 0) uniform image2D imgOutput;

void main() {
	vec4    = vec4(0.0, 0 at(teOurooord.x) + 0.01 * spd, w_WorkGidth) / (gl_NxeimxmWorkorkGroups.x * gl_zeriizkGSWomWe.x);
	value.y = float(telCmgageStore(iuptput, texelCooreed.y)/(gl_NuGroups.y*glroupSeu.y);

lCoord, value);
}